##### Hide the element in printed document:
```css
.dc-print--hide
```

##### Preserve background color in printed document (it fixes missing color in Firefox):
```css
.dc-print--preserve-background
```
